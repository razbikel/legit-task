const SUSPICIOUS_PREFIX = 'hacker'

const SUSPICIOUS_TIME_RANGE = {
    start: '14:00 pm',
    end: '16:00 pm'
}

const SUSPICIOUS_REPO_TIME = 10;

module.exports = {
    SUSPICIOUS_PREFIX,
    SUSPICIOUS_TIME_RANGE,
    SUSPICIOUS_REPO_TIME
}